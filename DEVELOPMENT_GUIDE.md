# 💻 دليل المطورين والبنية الهندسية (Development & Architecture Guide)

هذا الملف يوفر توثيقاً متقدماً للمبرمجين والمطورين الذين سيعملون على صيانة، تعديل، أو توسيع قدرات النظام مستقبلاً.

## 1. النظرة المعمارية (Architecture Overview)
تم تصميم هذا النظام حالياً كـ **Client-Side Single Page Application (SPA)** لضمان أسرع أداء ممكن في استجابة الواجهات دون الاعتماد المستمر على خوادم بعيدة، وهو ما يتناسب مع بيئة العمل المحلية أو السريعة في المختبرات الطبية.

## 2. إدارة الحالة (State Management)
*   **React Context API**: نظراً لحجم التطبيق الحالي، تم استخدام `AppContext.tsx` كبديل خفيف وسريع لأدوات مثل Redux.
*   يوفر السياق (Context) وصولاً شاملاً للمكونات للبيانات الأساسية:
    *   `reports`: مصفوفة كل التقارير المخبرية المعالجة.
    *   `patients`: أرشيف بيانات المراجعين.
    *   `activeScreen`: متغير نصي يتحكم في التنقل بين شاشات النظام لتجنب تعقيد توجيهات المسارات (React Router) في هذه المرحلة.

## 3. هندسة المكونات (Component Design Pattern)
يعتمد المشروع على مبدأ "فصل الاهتمامات" (Separation of Concerns):
*   **المكونات الذكية (Smart/Container Components):** مثل `DashboardScreen.tsx`، وظيفتها جلب البيانات من الـ Context وتمريرها وإدارة المنطق.
*   **المكونات الغبية/المرئية (Dumb/Presentational Components):** مثل `DashboardCharts.tsx` أو `DashboardKpis.tsx`، تعتمد بالكامل على الـ `Props` الممررة إليها وتتفرغ لعرض واجهة المستخدم والتفاعل المرئي فقط. هذا يسهل جداً عمليات الاختبار وإعادة الاستخدام.

## 4. إرشادات إضافة مكونات ورسوم بيانية جديدة (Adding New Features)
1.  **البيانات:** إذا كان المكون يحتاج لبيانات جديدة، قم بتعريف الواجهة (Interface) الخاصة بها في `src/types/index.ts`.
2.  **الرسوم البيانية (Recharts):**
    *   جميع مخططات الـ Dashboard مجمعة في `src/components/dashboard/DashboardCharts.tsx`.
    *   عند إضافة (Chart) جديد، قم بمعالجة البيانات وتجهيز المصفوفة المطلوبة قبل تمريرها لـ `<ResponsiveContainer>`.
    *   حافظ على التناسق اللوني المحدد للنظام: (أزرق داكن `#0b3b8c` للأساسي، أخضر `#10b981` للإنجاز، برتقالي للمراجعة، وأحمر للحالات الحرجة).
3.  **الشاشات (Screens):** لإضافة صفحة عمل جديدة:
    *   أنشئ ملف المكون في مسار `src/components/screens/`.
    *   أضف معرّف الشاشة الجديد في `ScreenType` داخل `src/types/index.ts`.
    *   عدّل `App.tsx` لعرض المكون الجديد عند تحديد مساره.

## 5. جودة الكود والأمان (Code Quality & Security)
*   **TypeScript:** الكود يعتمد بشكل صارم على الكتابة الثابتة (Static Typing). يُمنع استخدام `any` إلا في الحالات الاستثنائية القصوى.
*   **أمان واجهة المستخدم (UI Security):** لا تقم بوضع منطق أو حسابات سرية (مثل معادلات مالية متقدمة) حصرياً في الواجهة الأمامية إن كان سيتم رفع التطبيق للإنترنت المفتوح.
*   تم تطبيق زر الخصوصية (Privacy Mode) لإخفاء المعلومات المالية من واجهة المستخدم (DOM) لتأمينها من المتطفلين في صالة الاستقبال.

## 6. مسار التحديث المستقبلي (Roadmap & Scaling)
عند انتقال المختبر لمرحلة الربط السحابي الموسع (Cloud Backend):
1.  **قاعدة البيانات:** يُنصح بربط النظام بـ `Firebase Firestore` أو خادم `Node.js + PostgreSQL` عبر استبدال بيانات الـ `MockData` بطلبات `API` باستخدام `React Query` أو `Axios`.
2.  **المصادقة (Authentication):** بناء نظام تسجيل دخول (Login) لربط كل إجراء في النظام بمستخدم محدد لغرض المراجعة الطبية والمساءلة.
3.  **ربط أجهزة الفحص (LIS Middleware):** توفير واجهة API لاستقبال النتائج مباشرة من الأجهزة الطبية عبر بروتوكول HL7، وعرضها تلقائياً للمراجعة في شاشة "قيد الانتظار".
